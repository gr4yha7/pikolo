[profile.default]
src = "src"
out = "out"
libs = ["lib"]
evm_version = "paris"
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200
via_ir = false

[rpc_endpoints]
matsnet = "https://rpc.test.mezo.org"
sepolia = "${SEPOLIA_RPC_URL}"
mainnet = "${MAINNET_RPC_URL}"

[etherscan]
# Mezo testnet explorer verification (optional)
# matsnet = { api_url = "https://explorer.test.mezo.org/api", key = "${MEZO_EXPLORER_API_KEY}" }
